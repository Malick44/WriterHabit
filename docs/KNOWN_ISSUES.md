# Known Issues

Status: Release-readiness issue list from Prompt 27 final QA on 2026-06-09.

| ID | Severity | Area | Issue | Impact | Recommended Fix |
| --- | --- | --- | --- | --- | --- |
| WW-REL-001 | Resolved | Backend | Resolved 2026-06-11: `services/api/` now has a Fastify TypeScript runtime with package scripts, `/api/v1/health`, Supabase bearer JWT verification, request IDs, CORS, request logging, standard error middleware, authenticated session/profile smoke endpoints, fail-closed feature route shells, and integration tests. The runtime ignores client-writable `user_metadata.role` and uses trusted `app_metadata.role` or least-privileged `student`. | Runtime shell is no longer the blocker. Feature persistence, resource authorization, production migrations, provider integrations, and deployment remain tracked in the related backend blockers below. | Keep `cd services/api && npm run typecheck && npm test && npm run build` in backend release gates while implementing the remaining feature handlers. |
| WW-REL-002 | Resolved | Database and RLS | Resolved 2026-06-11: `scripts/supabase-migrations.mjs` now applies ordered SQL migrations with a checksum ledger in `public.writerhabit_schema_migrations`, and `services/api/tests/rls/resource-policy-verification.sql` verifies role/resource boundaries in a rollback-only transaction. `202606110001_server_owned_roles.sql` blocks public-client `public.users.role` changes and legacy auth-metadata role sync, and `202606110002_resource_rls_hardening.sql` removes broad public-client write policies for parent links, class rosters, AI/review logs, progress, badges, weekly reviews, and prepared notifications. `node scripts/supabase-migrations.mjs apply-and-verify` passed against the configured development Supabase. | The repo now has a repeatable migration/RLS gate. Production release still requires the owner/backend operator to run the same command with staging/production Supabase credentials and archive the run output without logging secrets. | Keep `node scripts/supabase-migrations.mjs apply-and-verify` and `node scripts/supabase-admin.mjs health` in database release gates; add catalog seed data and production feature persistence as the backend routes are completed. |
| WW-REL-003 | Review | Subscriptions | Round 3 implementation 2026-06-11: RevenueCat is selected as the iOS/Android entitlement provider. Backend entitlement reads, checkout intent, restore reconciliation, Authorization-verified idempotent webhooks, stale provider-event ordering guards, and advertised Plus-surface gates/server redaction are implemented locally. Migration `202606110006_subscription_event_ordering.sql` was applied and RLS verification passed against the configured development Supabase. | Paid access is no longer granted solely by mobile state, representative Plus surfaces fail closed or show upgrade/payment states, and delayed older RevenueCat events cannot re-enable access after newer refund, expiration, or billing-issue events. Store checkout launch is still blocked until owner-provided RevenueCat app keys/products and a native RevenueCat SDK rebuild are available; without `REVENUECAT_SECRET_API_KEY`, restore fails closed unless an active server entitlement already exists from a verified webhook. | Review round 3, run RevenueCat/App Store/Play Store sandbox purchase and restore QA with configured `REVENUECAT_WEBHOOK_AUTHORIZATION` and `REVENUECAT_SECRET_API_KEY`, include stale-event, transfer, and alias scenarios, then add the native RevenueCat SDK/store product flow in a rebuild before enabling paid plans publicly. |
| WW-REL-004 | P1 | QA Automation | Flow 1 and Flow 2 are documented and covered by provider-free unit/integration scaffolds, but no mobile E2E runner is configured. | Route transitions, device UI behavior, keyboard behavior, canvas gestures, and offline recovery are not automatically validated on installed builds. | Add Maestro or Detox and automate Flow 1, Flow 2, parent report review, teacher assignment creation, and paywall gates. |
| WW-REL-005 | P1 | Tooling | Resolved 2026-06-10: `eslint` + `eslint-config-expo` are installed with a flat config in `apps/mobile/eslint.config.js`, and `npm run lint` passes with zero errors and zero warnings. Reanimated shared-value mutation is exempted from `react-hooks/immutability` (documented in the config), and remaining query/prop-to-state sync effects carry inline justifications. | Lint is now an enforceable release gate. | Keep `npm run lint` in the release gate set. |
| WW-REL-006 | P1 | Build Surface | `./script/build_and_run.sh --export-web` fails because `react-native-web` is not installed. | Web export is currently not a valid release check. | Decide whether web is supported. Install web dependencies and test web export, or remove the web export command from release expectations. |
| WW-REL-007 | P1 | AI Runtime | Mobile AI coach still uses deterministic local mocks, and backend feedback review uses the deterministic mock provider behind the API service boundary. The backend AI service scaffolds are not connected to a production provider or durable worker queue. | Real AI responses, provider moderation, persisted rate/usage limits, durable review processing, and audit persistence are not operational. | Wire AI requests through production provider adapters, durable workers, persisted moderation/usage/audit stores, and provider observability. |
| WW-REL-008 | P1 | Canvas Storage | Canvas is local-first and signed-in metadata/stroke sync writes through backend routes, but real file export, object upload, signed URLs, and storage authorization are not implemented. | Editable canvas data can sync for signed-in sessions, but production attachment/export artifact workflows are not release-ready. | Implement signed upload/download endpoints, object path derivation, local file export, retry, and audit logging. |
| WW-REL-009 | P1 | Notifications | Local device scheduling, Expo token registration boundary, and framework-neutral backend push service exist, but production push delivery is not deployed. EAS project-id lookup now reads `Constants.easConfig?.projectId` or `Constants.expoConfig?.extra?.eas?.projectId` and skips placeholders safely. APNs/FCM credentials, worker scheduling, and parent/teacher report delivery are still incomplete. | Local student reminders can work in native builds when permissions allow, but production remote push delivery and report notifications are not release-ready. | Link the real EAS project id, deploy notification endpoints/workers, configure APNs/FCM through EAS, add delivery observability, and run native notification QA. |
| WW-REL-010 | P1 | Auth Profiles | Student edit/goals/language settings and signed-in student onboarding fields sync to Supabase, and mobile no longer trusts or writes client-mutable role/entitlement metadata. Broader role profiles, parent links, and teacher class state are still not fully persisted through production APIs. | Cross-device student profile settings improve, but parent/teacher approvals, links, class state, and full profile hydration remain incomplete for production. | Complete profile hydration APIs, parent-link and teacher-approval APIs, and migrate remaining local/demo metadata to server-backed records. |
| WW-REL-011 | P2 | Accessibility QA | Automated labels and localization guards pass, but VoiceOver, TalkBack, large text, high contrast, reduced motion, and keyboard/switch workflows have not been manually verified. | Students with accessibility needs may hit untested navigation or control issues. | Run manual accessibility QA across grade bands and fix screen-level issues. |
| WW-REL-012 | P2 | Release Operations | Improved 2026-06-11: `expo-updates`, OTA runtime/update config, app-local `eas.json`, EAS workflow, and GitHub Actions release gates now exist. EAS init is still blocked on the owner choosing between `malickb` and `ai-orbit-studio`, so `apps/mobile/app.json` contains `WriterHabit_EAS_PROJECT_ID_REQUIRED` until the owner runs `cd apps/mobile && npx eas-cli@latest init --force && npx eas-cli@latest update:configure --non-interactive`. Store metadata, screenshots, privacy labels, crash reporting, release rollback, and support workflows are still incomplete. | The app has a reproducible release surface, but public release cannot proceed until the real EAS project id, credentials, store assets, and operations procedures are complete. | Commit the real EAS project id/update URL after owner linking, configure credentials and store metadata, select observability, and document rollback/incident/support procedures. |

## Current Non-Blockers

- TypeScript, Jest, Expo Doctor, iOS export, and Android export passed in this QA pass.
- Generated native/build output folders were removed from the working tree after local checks.
- No Supabase admin credential values were added to app code or docs.
